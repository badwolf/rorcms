<% title "Roles" %>

<table>
  <tr>
    <th>Name</th>
    <th>Condition</th>
  </tr>
  <% @roles.each do |role| %>
    <tr>
      <td><%=h role.name %></td>
      <td><%=h role.condition %></td>
      <td><%= link_to "Show", role %></td>
      <td><%= link_to "Edit", edit_role_path(role) %></td>
      <td><%= link_to "Destroy", role, :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to "New Role", new_role_path %></p>
